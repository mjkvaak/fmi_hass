# Local CLI (Debian/glibc). Home Assistant Container 2026.9 is Alpine/musl:
# rasterio and OpenCV have no musllinux wheels, so the HA manifest omits them.
numpy>=1.24,<3
matplotlib>=3.7,<4
rasterio>=1.3,<2
pyproj>=3.4,<4
contextily>=1.4
Pillow>=9.2
requests>=2.28
paho-mqtt>=2.0
opencv-python-headless>=4.7
xyzservices>=2023.2
